/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12077 $ of $
 *
 * $Id:$
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "cltproTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table cltproTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * prevail-mib::cltproTable is subid 10 of modEoCMib.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.36186.8.10, length: 9
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * cltproTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * cltproTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
cltproTable_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:cltproTable:cltproTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup cltproTable undo.
     * set up cltproTable undo information, in preparation for a set.
     * Undo storage is in (* cltproRowStatus_val_ptr )*
     */

    return rc;
} /* cltproTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
cltproTable_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:cltproTable:cltproTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> cltproTable undo.
     * cltproTable undo information, in response to a failed set.
     * Undo storage is in (* cltproRowStatus_val_ptr )*
     */

    return rc;
} /* cltproTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
cltproTable_undo_cleanup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:cltproTable:cltproTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup cltproTable undo.
     * Undo storage is in (* cltproRowStatus_val_ptr )*
     */

    return rc;
} /* cltproTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * cltproTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param cltproTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
cltproTable_commit( cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:cltproTable:cltproTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit cltproTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_CLTPROBASE_FLAG) {
       save_flags &= ~COLUMN_CLTPROBASE_FLAG; /* clear cltproBase */
       /*
        * TODO:482:o: |-> commit column cltproBase.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROBASE_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproBase commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproBase
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROBASE_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROMACLIMIT_FLAG) {
       save_flags &= ~COLUMN_CLTPROMACLIMIT_FLAG; /* clear cltproMacLimit */
       /*
        * TODO:482:o: |-> commit column cltproMacLimit.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROMACLIMIT_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproMacLimit commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproMacLimit
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROMACLIMIT_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCURATE_FLAG) {
       save_flags &= ~COLUMN_CLTPROCURATE_FLAG; /* clear cltproCuRate */
       /*
        * TODO:482:o: |-> commit column cltproCuRate.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCURATE_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCuRate commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCuRate
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCURATE_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCDRATE_FLAG) {
       save_flags &= ~COLUMN_CLTPROCDRATE_FLAG; /* clear cltproCdRate */
       /*
        * TODO:482:o: |-> commit column cltproCdRate.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCDRATE_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCdRate commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCdRate
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCDRATE_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROLOAGTIME_FLAG) {
       save_flags &= ~COLUMN_CLTPROLOAGTIME_FLAG; /* clear cltproLoagTime */
       /*
        * TODO:482:o: |-> commit column cltproLoagTime.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROLOAGTIME_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproLoagTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproLoagTime
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROLOAGTIME_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROREAGTIME_FLAG) {
       save_flags &= ~COLUMN_CLTPROREAGTIME_FLAG; /* clear cltproReagTime */
       /*
        * TODO:482:o: |-> commit column cltproReagTime.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROREAGTIME_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproReagTime commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproReagTime
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROREAGTIME_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROIGMPPRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROIGMPPRI_FLAG; /* clear cltproIgmpPri */
       /*
        * TODO:482:o: |-> commit column cltproIgmpPri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROIGMPPRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproIgmpPri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproIgmpPri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROIGMPPRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROUNICASTPRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROUNICASTPRI_FLAG; /* clear cltproUnicastPri */
       /*
        * TODO:482:o: |-> commit column cltproUnicastPri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROUNICASTPRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproUnicastPri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproUnicastPri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROUNICASTPRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROAVSPRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROAVSPRI_FLAG; /* clear cltproAvsPri */
       /*
        * TODO:482:o: |-> commit column cltproAvsPri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROAVSPRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproAvsPri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproAvsPri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROAVSPRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROMCASTPRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROMCASTPRI_FLAG; /* clear cltproMcastPri */
       /*
        * TODO:482:o: |-> commit column cltproMcastPri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROMCASTPRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproMcastPri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproMcastPri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROMCASTPRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTBAPRISTS_FLAG) {
       save_flags &= ~COLUMN_CLTPROTBAPRISTS_FLAG; /* clear cltproTbaPriSts */
       /*
        * TODO:482:o: |-> commit column cltproTbaPriSts.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTBAPRISTS_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTbaPriSts commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTbaPriSts
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTBAPRISTS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOSPRISTS_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOSPRISTS_FLAG; /* clear cltproCosPriSts */
       /*
        * TODO:482:o: |-> commit column cltproCosPriSts.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOSPRISTS_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCosPriSts commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCosPriSts
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOSPRISTS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS0PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS0PRI_FLAG; /* clear cltproCos0pri */
       /*
        * TODO:482:o: |-> commit column cltproCos0pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS0PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos0pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos0pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS0PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS1PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS1PRI_FLAG; /* clear cltproCos1pri */
       /*
        * TODO:482:o: |-> commit column cltproCos1pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS1PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos1pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos1pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS1PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS2PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS2PRI_FLAG; /* clear cltproCos2pri */
       /*
        * TODO:482:o: |-> commit column cltproCos2pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS2PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos2pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos2pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS2PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS3PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS3PRI_FLAG; /* clear cltproCos3pri */
       /*
        * TODO:482:o: |-> commit column cltproCos3pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS3PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos3pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos3pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS3PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS4PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS4PRI_FLAG; /* clear cltproCos4pri */
       /*
        * TODO:482:o: |-> commit column cltproCos4pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS4PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos4pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos4pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS4PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS5PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS5PRI_FLAG; /* clear cltproCos5pri */
       /*
        * TODO:482:o: |-> commit column cltproCos5pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS5PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos5pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos5pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS5PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS6PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS6PRI_FLAG; /* clear cltproCos6pri */
       /*
        * TODO:482:o: |-> commit column cltproCos6pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS6PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos6pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos6pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS6PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOS7PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOS7PRI_FLAG; /* clear cltproCos7pri */
       /*
        * TODO:482:o: |-> commit column cltproCos7pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROCOS7PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCos7pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCos7pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOS7PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOSPRISTS_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOSPRISTS_FLAG; /* clear cltproTosPriSts */
       /*
        * TODO:482:o: |-> commit column cltproTosPriSts.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOSPRISTS_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTosPriSts commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTosPriSts
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOSPRISTS_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS0PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS0PRI_FLAG; /* clear cltproTos0pri */
       /*
        * TODO:482:o: |-> commit column cltproTos0pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS0PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos0pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos0pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS0PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS1PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS1PRI_FLAG; /* clear cltproTos1pri */
       /*
        * TODO:482:o: |-> commit column cltproTos1pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS1PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos1pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos1pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS1PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS2PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS2PRI_FLAG; /* clear cltproTos2pri */
       /*
        * TODO:482:o: |-> commit column cltproTos2pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS2PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos2pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos2pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS2PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS3PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS3PRI_FLAG; /* clear cltproTos3pri */
       /*
        * TODO:482:o: |-> commit column cltproTos3pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS3PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos3pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos3pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS3PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS4PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS4PRI_FLAG; /* clear cltproTos4pri */
       /*
        * TODO:482:o: |-> commit column cltproTos4pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS4PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos4pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos4pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS4PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS5PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS5PRI_FLAG; /* clear cltproTos5pri */
       /*
        * TODO:482:o: |-> commit column cltproTos5pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS5PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos5pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos5pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS5PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS6PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS6PRI_FLAG; /* clear cltproTos6pri */
       /*
        * TODO:482:o: |-> commit column cltproTos6pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS6PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos6pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos6pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS6PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROTOS7PRI_FLAG) {
       save_flags &= ~COLUMN_CLTPROTOS7PRI_FLAG; /* clear cltproTos7pri */
       /*
        * TODO:482:o: |-> commit column cltproTos7pri.
        */
       /* Modified by frank */
	//rc = -1;
	printf("	COLUMN_CLTPROTOS7PRI_FLAG\n");
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproTos7pri commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproTos7pri
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROTOS7PRI_FLAG;
       }
    }

    if (save_flags & COLUMN_CLTPROCOMMIT_FLAG) {
       save_flags &= ~COLUMN_CLTPROCOMMIT_FLAG; /* clear cltproCommit */
       /*
        * TODO:482:o: |-> commit column cltproCommit.
        */
       /* Modified by frank */
	//rc = -1;
	printf("-->clt profile %d commit = %d\n", rowreq_ctx->data.cltproIndex, rowreq_ctx->data.cltproCommit);
	rc = 0;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"cltproTable column cltproCommit commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo cltproCommit
             */
            rowreq_ctx->column_set_flags |= COLUMN_CLTPROCOMMIT_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* cltproTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * cltproTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param cltproTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
cltproTable_undo_commit( cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:cltproTable:cltproTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo cltproTable commit.
     * check the column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN__FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* cltproTable_undo_commit */

/*
 * TODO:440:M: Implement cltproTable node value checks.
 * TODO:450:M: Implement cltproTable undo functions.
 * TODO:460:M: Implement cltproTable set functions.
 * TODO:480:M: Implement cltproTable commit functions.
 */
/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproBase
 * cltproBase is subid 3 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.3
 * Description:
atheros eoc pib index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 32;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproBase_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is in (one of) the range set(s):  1 - 32
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproBase_check_value( cltproTable_rowreq_ctx *rowreq_ctx, long cltproBase_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproBase_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproBase value.
     */

    return MFD_SUCCESS; /* cltproBase value not illegal */
} /* cltproBase_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproBase_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproBase_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproBase undo.
     */
    /*
     * copy cltproBase data
     * set rowreq_ctx->undo->cltproBase from rowreq_ctx->data.cltproBase
     */
    rowreq_ctx->undo->cltproBase = rowreq_ctx->data.cltproBase;


    return MFD_SUCCESS;
} /* cltproBase_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproBase_val
 *        A long containing the new value.
 */
int
cltproBase_set( cltproTable_rowreq_ctx *rowreq_ctx, long cltproBase_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproBase_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproBase value.
     * set cltproBase value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproBase = cltproBase_val;

    return MFD_SUCCESS;
} /* cltproBase_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproBase_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproBase_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproBase undo.
     */
    /*
     * copy cltproBase data
     * set rowreq_ctx->data.cltproBase from rowreq_ctx->undo->cltproBase
     */
    rowreq_ctx->data.cltproBase = rowreq_ctx->undo->cltproBase;

    
    return MFD_SUCCESS;
} /* cltproBase_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproMacLimit
 * cltproMacLimit is subid 4 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.4
 * Description:
EoC bridged hosts limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 8/16. Values:  hosts_limiting_disabled(0), one_host(1), two_hosts(2), three_hosts(3), four_hosts(4), five_hosts(5), six_hosts(6), seven_hosts(7), eight_hosts(8), null_hosts(65)
 *
 * Its syntax is HostsLimitingValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproMacLimit_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  hosts_limiting_disabled(0), one_host(1), two_hosts(2), three_hosts(3), four_hosts(4), five_hosts(5), six_hosts(6), seven_hosts(7), eight_hosts(8), null_hosts(65)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproMacLimit_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproMacLimit_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproMacLimit_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproMacLimit value.
     */

    return MFD_SUCCESS; /* cltproMacLimit value not illegal */
} /* cltproMacLimit_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproMacLimit_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproMacLimit_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproMacLimit undo.
     */
    /*
     * copy cltproMacLimit data
     * set rowreq_ctx->undo->cltproMacLimit from rowreq_ctx->data.cltproMacLimit
     */
    rowreq_ctx->undo->cltproMacLimit = rowreq_ctx->data.cltproMacLimit;


    return MFD_SUCCESS;
} /* cltproMacLimit_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproMacLimit_val
 *        A long containing the new value.
 */
int
cltproMacLimit_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproMacLimit_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproMacLimit_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproMacLimit value.
     * set cltproMacLimit value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproMacLimit = cltproMacLimit_val;

    return MFD_SUCCESS;
} /* cltproMacLimit_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproMacLimit_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproMacLimit_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproMacLimit undo.
     */
    /*
     * copy cltproMacLimit data
     * set rowreq_ctx->data.cltproMacLimit from rowreq_ctx->undo->cltproMacLimit
     */
    rowreq_ctx->data.cltproMacLimit = rowreq_ctx->undo->cltproMacLimit;

    
    return MFD_SUCCESS;
} /* cltproMacLimit_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCuRate
 * cltproCuRate is subid 5 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.5
 * Description:
Cable uplink rate limiting in kbps.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCuRate_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCuRate_check_value( cltproTable_rowreq_ctx *rowreq_ctx, long cltproCuRate_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCuRate_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCuRate value.
     */

    return MFD_SUCCESS; /* cltproCuRate value not illegal */
} /* cltproCuRate_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCuRate_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCuRate_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCuRate undo.
     */
    /*
     * copy cltproCuRate data
     * set rowreq_ctx->undo->cltproCuRate from rowreq_ctx->data.cltproCuRate
     */
    rowreq_ctx->undo->cltproCuRate = rowreq_ctx->data.cltproCuRate;


    return MFD_SUCCESS;
} /* cltproCuRate_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCuRate_val
 *        A long containing the new value.
 */
int
cltproCuRate_set( cltproTable_rowreq_ctx *rowreq_ctx, long cltproCuRate_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCuRate_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCuRate value.
     * set cltproCuRate value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCuRate = cltproCuRate_val;

    return MFD_SUCCESS;
} /* cltproCuRate_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCuRate_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCuRate_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCuRate undo.
     */
    /*
     * copy cltproCuRate data
     * set rowreq_ctx->data.cltproCuRate from rowreq_ctx->undo->cltproCuRate
     */
    rowreq_ctx->data.cltproCuRate = rowreq_ctx->undo->cltproCuRate;

    
    return MFD_SUCCESS;
} /* cltproCuRate_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCdRate
 * cltproCdRate is subid 6 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.6
 * Description:
Cable downlink rate limiting in kbps.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCdRate_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCdRate_check_value( cltproTable_rowreq_ctx *rowreq_ctx, long cltproCdRate_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCdRate_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCdRate value.
     */

    return MFD_SUCCESS; /* cltproCdRate value not illegal */
} /* cltproCdRate_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCdRate_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCdRate_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCdRate undo.
     */
    /*
     * copy cltproCdRate data
     * set rowreq_ctx->undo->cltproCdRate from rowreq_ctx->data.cltproCdRate
     */
    rowreq_ctx->undo->cltproCdRate = rowreq_ctx->data.cltproCdRate;


    return MFD_SUCCESS;
} /* cltproCdRate_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCdRate_val
 *        A long containing the new value.
 */
int
cltproCdRate_set( cltproTable_rowreq_ctx *rowreq_ctx, long cltproCdRate_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCdRate_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCdRate value.
     * set cltproCdRate value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCdRate = cltproCdRate_val;

    return MFD_SUCCESS;
} /* cltproCdRate_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCdRate_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCdRate_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCdRate undo.
     */
    /*
     * copy cltproCdRate data
     * set rowreq_ctx->data.cltproCdRate from rowreq_ctx->undo->cltproCdRate
     */
    rowreq_ctx->data.cltproCdRate = rowreq_ctx->undo->cltproCdRate;

    
    return MFD_SUCCESS;
} /* cltproCdRate_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproLoagTime
 * cltproLoagTime is subid 7 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.7
 * Description:
Local bridge aging time in minutes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproLoagTime_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproLoagTime_check_value( cltproTable_rowreq_ctx *rowreq_ctx, long cltproLoagTime_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproLoagTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproLoagTime value.
     */

    return MFD_SUCCESS; /* cltproLoagTime value not illegal */
} /* cltproLoagTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproLoagTime_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproLoagTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproLoagTime undo.
     */
    /*
     * copy cltproLoagTime data
     * set rowreq_ctx->undo->cltproLoagTime from rowreq_ctx->data.cltproLoagTime
     */
    rowreq_ctx->undo->cltproLoagTime = rowreq_ctx->data.cltproLoagTime;


    return MFD_SUCCESS;
} /* cltproLoagTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproLoagTime_val
 *        A long containing the new value.
 */
int
cltproLoagTime_set( cltproTable_rowreq_ctx *rowreq_ctx, long cltproLoagTime_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproLoagTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproLoagTime value.
     * set cltproLoagTime value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproLoagTime = cltproLoagTime_val;

    return MFD_SUCCESS;
} /* cltproLoagTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproLoagTime_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproLoagTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproLoagTime undo.
     */
    /*
     * copy cltproLoagTime data
     * set rowreq_ctx->data.cltproLoagTime from rowreq_ctx->undo->cltproLoagTime
     */
    rowreq_ctx->data.cltproLoagTime = rowreq_ctx->undo->cltproLoagTime;

    
    return MFD_SUCCESS;
} /* cltproLoagTime_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproReagTime
 * cltproReagTime is subid 8 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.8
 * Description:
Remote devie bridge table aging time in minutes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproReagTime_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproReagTime_check_value( cltproTable_rowreq_ctx *rowreq_ctx, long cltproReagTime_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproReagTime_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproReagTime value.
     */

    return MFD_SUCCESS; /* cltproReagTime value not illegal */
} /* cltproReagTime_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproReagTime_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproReagTime_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproReagTime undo.
     */
    /*
     * copy cltproReagTime data
     * set rowreq_ctx->undo->cltproReagTime from rowreq_ctx->data.cltproReagTime
     */
    rowreq_ctx->undo->cltproReagTime = rowreq_ctx->data.cltproReagTime;


    return MFD_SUCCESS;
} /* cltproReagTime_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproReagTime_val
 *        A long containing the new value.
 */
int
cltproReagTime_set( cltproTable_rowreq_ctx *rowreq_ctx, long cltproReagTime_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproReagTime_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproReagTime value.
     * set cltproReagTime value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproReagTime = cltproReagTime_val;

    return MFD_SUCCESS;
} /* cltproReagTime_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproReagTime_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproReagTime_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproReagTime undo.
     */
    /*
     * copy cltproReagTime data
     * set rowreq_ctx->data.cltproReagTime from rowreq_ctx->undo->cltproReagTime
     */
    rowreq_ctx->data.cltproReagTime = rowreq_ctx->undo->cltproReagTime;

    
    return MFD_SUCCESS;
} /* cltproReagTime_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproIgmpPri
 * cltproIgmpPri is subid 9 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.9
 * Description:
IGMP Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproIgmpPri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproIgmpPri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproIgmpPri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproIgmpPri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproIgmpPri value.
     */

    return MFD_SUCCESS; /* cltproIgmpPri value not illegal */
} /* cltproIgmpPri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproIgmpPri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproIgmpPri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproIgmpPri undo.
     */
    /*
     * copy cltproIgmpPri data
     * set rowreq_ctx->undo->cltproIgmpPri from rowreq_ctx->data.cltproIgmpPri
     */
    rowreq_ctx->undo->cltproIgmpPri = rowreq_ctx->data.cltproIgmpPri;


    return MFD_SUCCESS;
} /* cltproIgmpPri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproIgmpPri_val
 *        A long containing the new value.
 */
int
cltproIgmpPri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproIgmpPri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproIgmpPri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproIgmpPri value.
     * set cltproIgmpPri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproIgmpPri = cltproIgmpPri_val;

    return MFD_SUCCESS;
} /* cltproIgmpPri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproIgmpPri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproIgmpPri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproIgmpPri undo.
     */
    /*
     * copy cltproIgmpPri data
     * set rowreq_ctx->data.cltproIgmpPri from rowreq_ctx->undo->cltproIgmpPri
     */
    rowreq_ctx->data.cltproIgmpPri = rowreq_ctx->undo->cltproIgmpPri;

    
    return MFD_SUCCESS;
} /* cltproIgmpPri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproUnicastPri
 * cltproUnicastPri is subid 10 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.10
 * Description:
Unicast Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproUnicastPri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproUnicastPri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproUnicastPri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproUnicastPri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproUnicastPri value.
     */

    return MFD_SUCCESS; /* cltproUnicastPri value not illegal */
} /* cltproUnicastPri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproUnicastPri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproUnicastPri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproUnicastPri undo.
     */
    /*
     * copy cltproUnicastPri data
     * set rowreq_ctx->undo->cltproUnicastPri from rowreq_ctx->data.cltproUnicastPri
     */
    rowreq_ctx->undo->cltproUnicastPri = rowreq_ctx->data.cltproUnicastPri;


    return MFD_SUCCESS;
} /* cltproUnicastPri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproUnicastPri_val
 *        A long containing the new value.
 */
int
cltproUnicastPri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproUnicastPri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproUnicastPri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproUnicastPri value.
     * set cltproUnicastPri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproUnicastPri = cltproUnicastPri_val;

    return MFD_SUCCESS;
} /* cltproUnicastPri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproUnicastPri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproUnicastPri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproUnicastPri undo.
     */
    /*
     * copy cltproUnicastPri data
     * set rowreq_ctx->data.cltproUnicastPri from rowreq_ctx->undo->cltproUnicastPri
     */
    rowreq_ctx->data.cltproUnicastPri = rowreq_ctx->undo->cltproUnicastPri;

    
    return MFD_SUCCESS;
} /* cltproUnicastPri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproAvsPri
 * cltproAvsPri is subid 11 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.11
 * Description:
AV Stream Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproAvsPri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproAvsPri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproAvsPri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproAvsPri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproAvsPri value.
     */

    return MFD_SUCCESS; /* cltproAvsPri value not illegal */
} /* cltproAvsPri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproAvsPri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproAvsPri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproAvsPri undo.
     */
    /*
     * copy cltproAvsPri data
     * set rowreq_ctx->undo->cltproAvsPri from rowreq_ctx->data.cltproAvsPri
     */
    rowreq_ctx->undo->cltproAvsPri = rowreq_ctx->data.cltproAvsPri;


    return MFD_SUCCESS;
} /* cltproAvsPri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproAvsPri_val
 *        A long containing the new value.
 */
int
cltproAvsPri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproAvsPri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproAvsPri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproAvsPri value.
     * set cltproAvsPri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproAvsPri = cltproAvsPri_val;

    return MFD_SUCCESS;
} /* cltproAvsPri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproAvsPri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproAvsPri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproAvsPri undo.
     */
    /*
     * copy cltproAvsPri data
     * set rowreq_ctx->data.cltproAvsPri from rowreq_ctx->undo->cltproAvsPri
     */
    rowreq_ctx->data.cltproAvsPri = rowreq_ctx->undo->cltproAvsPri;

    
    return MFD_SUCCESS;
} /* cltproAvsPri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproMcastPri
 * cltproMcastPri is subid 12 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.12
 * Description:
Multicast Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproMcastPri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproMcastPri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproMcastPri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproMcastPri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproMcastPri value.
     */

    return MFD_SUCCESS; /* cltproMcastPri value not illegal */
} /* cltproMcastPri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproMcastPri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproMcastPri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproMcastPri undo.
     */
    /*
     * copy cltproMcastPri data
     * set rowreq_ctx->undo->cltproMcastPri from rowreq_ctx->data.cltproMcastPri
     */
    rowreq_ctx->undo->cltproMcastPri = rowreq_ctx->data.cltproMcastPri;


    return MFD_SUCCESS;
} /* cltproMcastPri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproMcastPri_val
 *        A long containing the new value.
 */
int
cltproMcastPri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproMcastPri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproMcastPri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproMcastPri value.
     * set cltproMcastPri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproMcastPri = cltproMcastPri_val;

    return MFD_SUCCESS;
} /* cltproMcastPri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproMcastPri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproMcastPri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproMcastPri undo.
     */
    /*
     * copy cltproMcastPri data
     * set rowreq_ctx->data.cltproMcastPri from rowreq_ctx->undo->cltproMcastPri
     */
    rowreq_ctx->data.cltproMcastPri = rowreq_ctx->undo->cltproMcastPri;

    
    return MFD_SUCCESS;
} /* cltproMcastPri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTbaPriSts
 * cltproTbaPriSts is subid 13 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.13
 * Description:
TX buffer based on priority status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTbaPriSts_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTbaPriSts_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTbaPriSts_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTbaPriSts_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTbaPriSts value.
     */

    return MFD_SUCCESS; /* cltproTbaPriSts value not illegal */
} /* cltproTbaPriSts_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTbaPriSts_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTbaPriSts_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTbaPriSts undo.
     */
    /*
     * copy cltproTbaPriSts data
     * set rowreq_ctx->undo->cltproTbaPriSts from rowreq_ctx->data.cltproTbaPriSts
     */
    rowreq_ctx->undo->cltproTbaPriSts = rowreq_ctx->data.cltproTbaPriSts;


    return MFD_SUCCESS;
} /* cltproTbaPriSts_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTbaPriSts_val
 *        A long containing the new value.
 */
int
cltproTbaPriSts_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTbaPriSts_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTbaPriSts_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTbaPriSts value.
     * set cltproTbaPriSts value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTbaPriSts = cltproTbaPriSts_val;

    return MFD_SUCCESS;
} /* cltproTbaPriSts_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTbaPriSts_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTbaPriSts_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTbaPriSts undo.
     */
    /*
     * copy cltproTbaPriSts data
     * set rowreq_ctx->data.cltproTbaPriSts from rowreq_ctx->undo->cltproTbaPriSts
     */
    rowreq_ctx->data.cltproTbaPriSts = rowreq_ctx->undo->cltproTbaPriSts;

    
    return MFD_SUCCESS;
} /* cltproTbaPriSts_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCosPriSts
 * cltproCosPriSts is subid 14 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.14
 * Description:
Map VLAN to Priority CAP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCosPriSts_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCosPriSts_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCosPriSts_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCosPriSts_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCosPriSts value.
     */

    return MFD_SUCCESS; /* cltproCosPriSts value not illegal */
} /* cltproCosPriSts_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCosPriSts_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCosPriSts_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCosPriSts undo.
     */
    /*
     * copy cltproCosPriSts data
     * set rowreq_ctx->undo->cltproCosPriSts from rowreq_ctx->data.cltproCosPriSts
     */
    rowreq_ctx->undo->cltproCosPriSts = rowreq_ctx->data.cltproCosPriSts;


    return MFD_SUCCESS;
} /* cltproCosPriSts_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCosPriSts_val
 *        A long containing the new value.
 */
int
cltproCosPriSts_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCosPriSts_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCosPriSts_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCosPriSts value.
     * set cltproCosPriSts value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCosPriSts = cltproCosPriSts_val;

    return MFD_SUCCESS;
} /* cltproCosPriSts_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCosPriSts_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCosPriSts_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCosPriSts undo.
     */
    /*
     * copy cltproCosPriSts data
     * set rowreq_ctx->data.cltproCosPriSts from rowreq_ctx->undo->cltproCosPriSts
     */
    rowreq_ctx->data.cltproCosPriSts = rowreq_ctx->undo->cltproCosPriSts;

    
    return MFD_SUCCESS;
} /* cltproCosPriSts_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos0pri
 * cltproCos0pri is subid 15 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.15
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos0pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos0pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos0pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos0pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos0pri value.
     */

    return MFD_SUCCESS; /* cltproCos0pri value not illegal */
} /* cltproCos0pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos0pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos0pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos0pri undo.
     */
    /*
     * copy cltproCos0pri data
     * set rowreq_ctx->undo->cltproCos0pri from rowreq_ctx->data.cltproCos0pri
     */
    rowreq_ctx->undo->cltproCos0pri = rowreq_ctx->data.cltproCos0pri;


    return MFD_SUCCESS;
} /* cltproCos0pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos0pri_val
 *        A long containing the new value.
 */
int
cltproCos0pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos0pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos0pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos0pri value.
     * set cltproCos0pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos0pri = cltproCos0pri_val;

    return MFD_SUCCESS;
} /* cltproCos0pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos0pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos0pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos0pri undo.
     */
    /*
     * copy cltproCos0pri data
     * set rowreq_ctx->data.cltproCos0pri from rowreq_ctx->undo->cltproCos0pri
     */
    rowreq_ctx->data.cltproCos0pri = rowreq_ctx->undo->cltproCos0pri;

    
    return MFD_SUCCESS;
} /* cltproCos0pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos1pri
 * cltproCos1pri is subid 16 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.16
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos1pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos1pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos1pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos1pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos1pri value.
     */

    return MFD_SUCCESS; /* cltproCos1pri value not illegal */
} /* cltproCos1pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos1pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos1pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos1pri undo.
     */
    /*
     * copy cltproCos1pri data
     * set rowreq_ctx->undo->cltproCos1pri from rowreq_ctx->data.cltproCos1pri
     */
    rowreq_ctx->undo->cltproCos1pri = rowreq_ctx->data.cltproCos1pri;


    return MFD_SUCCESS;
} /* cltproCos1pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos1pri_val
 *        A long containing the new value.
 */
int
cltproCos1pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos1pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos1pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos1pri value.
     * set cltproCos1pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos1pri = cltproCos1pri_val;

    return MFD_SUCCESS;
} /* cltproCos1pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos1pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos1pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos1pri undo.
     */
    /*
     * copy cltproCos1pri data
     * set rowreq_ctx->data.cltproCos1pri from rowreq_ctx->undo->cltproCos1pri
     */
    rowreq_ctx->data.cltproCos1pri = rowreq_ctx->undo->cltproCos1pri;

    
    return MFD_SUCCESS;
} /* cltproCos1pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos2pri
 * cltproCos2pri is subid 17 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.17
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos2pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos2pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos2pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos2pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos2pri value.
     */

    return MFD_SUCCESS; /* cltproCos2pri value not illegal */
} /* cltproCos2pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos2pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos2pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos2pri undo.
     */
    /*
     * copy cltproCos2pri data
     * set rowreq_ctx->undo->cltproCos2pri from rowreq_ctx->data.cltproCos2pri
     */
    rowreq_ctx->undo->cltproCos2pri = rowreq_ctx->data.cltproCos2pri;


    return MFD_SUCCESS;
} /* cltproCos2pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos2pri_val
 *        A long containing the new value.
 */
int
cltproCos2pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos2pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos2pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos2pri value.
     * set cltproCos2pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos2pri = cltproCos2pri_val;

    return MFD_SUCCESS;
} /* cltproCos2pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos2pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos2pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos2pri undo.
     */
    /*
     * copy cltproCos2pri data
     * set rowreq_ctx->data.cltproCos2pri from rowreq_ctx->undo->cltproCos2pri
     */
    rowreq_ctx->data.cltproCos2pri = rowreq_ctx->undo->cltproCos2pri;

    
    return MFD_SUCCESS;
} /* cltproCos2pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos3pri
 * cltproCos3pri is subid 18 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.18
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos3pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos3pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos3pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos3pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos3pri value.
     */

    return MFD_SUCCESS; /* cltproCos3pri value not illegal */
} /* cltproCos3pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos3pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos3pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos3pri undo.
     */
    /*
     * copy cltproCos3pri data
     * set rowreq_ctx->undo->cltproCos3pri from rowreq_ctx->data.cltproCos3pri
     */
    rowreq_ctx->undo->cltproCos3pri = rowreq_ctx->data.cltproCos3pri;


    return MFD_SUCCESS;
} /* cltproCos3pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos3pri_val
 *        A long containing the new value.
 */
int
cltproCos3pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos3pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos3pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos3pri value.
     * set cltproCos3pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos3pri = cltproCos3pri_val;

    return MFD_SUCCESS;
} /* cltproCos3pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos3pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos3pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos3pri undo.
     */
    /*
     * copy cltproCos3pri data
     * set rowreq_ctx->data.cltproCos3pri from rowreq_ctx->undo->cltproCos3pri
     */
    rowreq_ctx->data.cltproCos3pri = rowreq_ctx->undo->cltproCos3pri;

    
    return MFD_SUCCESS;
} /* cltproCos3pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos4pri
 * cltproCos4pri is subid 19 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.19
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos4pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos4pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos4pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos4pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos4pri value.
     */

    return MFD_SUCCESS; /* cltproCos4pri value not illegal */
} /* cltproCos4pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos4pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos4pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos4pri undo.
     */
    /*
     * copy cltproCos4pri data
     * set rowreq_ctx->undo->cltproCos4pri from rowreq_ctx->data.cltproCos4pri
     */
    rowreq_ctx->undo->cltproCos4pri = rowreq_ctx->data.cltproCos4pri;


    return MFD_SUCCESS;
} /* cltproCos4pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos4pri_val
 *        A long containing the new value.
 */
int
cltproCos4pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos4pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos4pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos4pri value.
     * set cltproCos4pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos4pri = cltproCos4pri_val;

    return MFD_SUCCESS;
} /* cltproCos4pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos4pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos4pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos4pri undo.
     */
    /*
     * copy cltproCos4pri data
     * set rowreq_ctx->data.cltproCos4pri from rowreq_ctx->undo->cltproCos4pri
     */
    rowreq_ctx->data.cltproCos4pri = rowreq_ctx->undo->cltproCos4pri;

    
    return MFD_SUCCESS;
} /* cltproCos4pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos5pri
 * cltproCos5pri is subid 20 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.20
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos5pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos5pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos5pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos5pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos5pri value.
     */

    return MFD_SUCCESS; /* cltproCos5pri value not illegal */
} /* cltproCos5pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos5pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos5pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos5pri undo.
     */
    /*
     * copy cltproCos5pri data
     * set rowreq_ctx->undo->cltproCos5pri from rowreq_ctx->data.cltproCos5pri
     */
    rowreq_ctx->undo->cltproCos5pri = rowreq_ctx->data.cltproCos5pri;


    return MFD_SUCCESS;
} /* cltproCos5pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos5pri_val
 *        A long containing the new value.
 */
int
cltproCos5pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos5pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos5pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos5pri value.
     * set cltproCos5pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos5pri = cltproCos5pri_val;

    return MFD_SUCCESS;
} /* cltproCos5pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos5pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos5pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos5pri undo.
     */
    /*
     * copy cltproCos5pri data
     * set rowreq_ctx->data.cltproCos5pri from rowreq_ctx->undo->cltproCos5pri
     */
    rowreq_ctx->data.cltproCos5pri = rowreq_ctx->undo->cltproCos5pri;

    
    return MFD_SUCCESS;
} /* cltproCos5pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos6pri
 * cltproCos6pri is subid 21 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.21
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos6pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos6pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos6pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos6pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos6pri value.
     */

    return MFD_SUCCESS; /* cltproCos6pri value not illegal */
} /* cltproCos6pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos6pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos6pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos6pri undo.
     */
    /*
     * copy cltproCos6pri data
     * set rowreq_ctx->undo->cltproCos6pri from rowreq_ctx->data.cltproCos6pri
     */
    rowreq_ctx->undo->cltproCos6pri = rowreq_ctx->data.cltproCos6pri;


    return MFD_SUCCESS;
} /* cltproCos6pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos6pri_val
 *        A long containing the new value.
 */
int
cltproCos6pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos6pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos6pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos6pri value.
     * set cltproCos6pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos6pri = cltproCos6pri_val;

    return MFD_SUCCESS;
} /* cltproCos6pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos6pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos6pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos6pri undo.
     */
    /*
     * copy cltproCos6pri data
     * set rowreq_ctx->data.cltproCos6pri from rowreq_ctx->undo->cltproCos6pri
     */
    rowreq_ctx->data.cltproCos6pri = rowreq_ctx->undo->cltproCos6pri;

    
    return MFD_SUCCESS;
} /* cltproCos6pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCos7pri
 * cltproCos7pri is subid 22 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.22
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCos7pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCos7pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos7pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos7pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCos7pri value.
     */

    return MFD_SUCCESS; /* cltproCos7pri value not illegal */
} /* cltproCos7pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCos7pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCos7pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCos7pri undo.
     */
    /*
     * copy cltproCos7pri data
     * set rowreq_ctx->undo->cltproCos7pri from rowreq_ctx->data.cltproCos7pri
     */
    rowreq_ctx->undo->cltproCos7pri = rowreq_ctx->data.cltproCos7pri;


    return MFD_SUCCESS;
} /* cltproCos7pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCos7pri_val
 *        A long containing the new value.
 */
int
cltproCos7pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCos7pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos7pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCos7pri value.
     * set cltproCos7pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCos7pri = cltproCos7pri_val;

    return MFD_SUCCESS;
} /* cltproCos7pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCos7pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCos7pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCos7pri undo.
     */
    /*
     * copy cltproCos7pri data
     * set rowreq_ctx->data.cltproCos7pri from rowreq_ctx->undo->cltproCos7pri
     */
    rowreq_ctx->data.cltproCos7pri = rowreq_ctx->undo->cltproCos7pri;

    
    return MFD_SUCCESS;
} /* cltproCos7pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTosPriSts
 * cltproTosPriSts is subid 23 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.23
 * Description:
Map TOS to Priority CAP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTosPriSts_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTosPriSts_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTosPriSts_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTosPriSts_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTosPriSts value.
     */

    return MFD_SUCCESS; /* cltproTosPriSts value not illegal */
} /* cltproTosPriSts_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTosPriSts_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTosPriSts_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTosPriSts undo.
     */
    /*
     * copy cltproTosPriSts data
     * set rowreq_ctx->undo->cltproTosPriSts from rowreq_ctx->data.cltproTosPriSts
     */
    rowreq_ctx->undo->cltproTosPriSts = rowreq_ctx->data.cltproTosPriSts;


    return MFD_SUCCESS;
} /* cltproTosPriSts_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTosPriSts_val
 *        A long containing the new value.
 */
int
cltproTosPriSts_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTosPriSts_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTosPriSts_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTosPriSts value.
     * set cltproTosPriSts value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTosPriSts = cltproTosPriSts_val;

    return MFD_SUCCESS;
} /* cltproTosPriSts_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTosPriSts_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTosPriSts_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTosPriSts undo.
     */
    /*
     * copy cltproTosPriSts data
     * set rowreq_ctx->data.cltproTosPriSts from rowreq_ctx->undo->cltproTosPriSts
     */
    rowreq_ctx->data.cltproTosPriSts = rowreq_ctx->undo->cltproTosPriSts;

    
    return MFD_SUCCESS;
} /* cltproTosPriSts_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos0pri
 * cltproTos0pri is subid 24 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.24
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos0pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos0pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos0pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos0pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos0pri value.
     */

    return MFD_SUCCESS; /* cltproTos0pri value not illegal */
} /* cltproTos0pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos0pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos0pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos0pri undo.
     */
    /*
     * copy cltproTos0pri data
     * set rowreq_ctx->undo->cltproTos0pri from rowreq_ctx->data.cltproTos0pri
     */
    rowreq_ctx->undo->cltproTos0pri = rowreq_ctx->data.cltproTos0pri;


    return MFD_SUCCESS;
} /* cltproTos0pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos0pri_val
 *        A long containing the new value.
 */
int
cltproTos0pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos0pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos0pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos0pri value.
     * set cltproTos0pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos0pri = cltproTos0pri_val;

    return MFD_SUCCESS;
} /* cltproTos0pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos0pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos0pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos0pri undo.
     */
    /*
     * copy cltproTos0pri data
     * set rowreq_ctx->data.cltproTos0pri from rowreq_ctx->undo->cltproTos0pri
     */
    rowreq_ctx->data.cltproTos0pri = rowreq_ctx->undo->cltproTos0pri;

    
    return MFD_SUCCESS;
} /* cltproTos0pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos1pri
 * cltproTos1pri is subid 25 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.25
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos1pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos1pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos1pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos1pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos1pri value.
     */

    return MFD_SUCCESS; /* cltproTos1pri value not illegal */
} /* cltproTos1pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos1pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos1pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos1pri undo.
     */
    /*
     * copy cltproTos1pri data
     * set rowreq_ctx->undo->cltproTos1pri from rowreq_ctx->data.cltproTos1pri
     */
    rowreq_ctx->undo->cltproTos1pri = rowreq_ctx->data.cltproTos1pri;


    return MFD_SUCCESS;
} /* cltproTos1pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos1pri_val
 *        A long containing the new value.
 */
int
cltproTos1pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos1pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos1pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos1pri value.
     * set cltproTos1pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos1pri = cltproTos1pri_val;

    return MFD_SUCCESS;
} /* cltproTos1pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos1pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos1pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos1pri undo.
     */
    /*
     * copy cltproTos1pri data
     * set rowreq_ctx->data.cltproTos1pri from rowreq_ctx->undo->cltproTos1pri
     */
    rowreq_ctx->data.cltproTos1pri = rowreq_ctx->undo->cltproTos1pri;

    
    return MFD_SUCCESS;
} /* cltproTos1pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos2pri
 * cltproTos2pri is subid 26 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.26
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos2pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos2pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos2pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos2pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos2pri value.
     */

    return MFD_SUCCESS; /* cltproTos2pri value not illegal */
} /* cltproTos2pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos2pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos2pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos2pri undo.
     */
    /*
     * copy cltproTos2pri data
     * set rowreq_ctx->undo->cltproTos2pri from rowreq_ctx->data.cltproTos2pri
     */
    rowreq_ctx->undo->cltproTos2pri = rowreq_ctx->data.cltproTos2pri;


    return MFD_SUCCESS;
} /* cltproTos2pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos2pri_val
 *        A long containing the new value.
 */
int
cltproTos2pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos2pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos2pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos2pri value.
     * set cltproTos2pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos2pri = cltproTos2pri_val;

    return MFD_SUCCESS;
} /* cltproTos2pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos2pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos2pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos2pri undo.
     */
    /*
     * copy cltproTos2pri data
     * set rowreq_ctx->data.cltproTos2pri from rowreq_ctx->undo->cltproTos2pri
     */
    rowreq_ctx->data.cltproTos2pri = rowreq_ctx->undo->cltproTos2pri;

    
    return MFD_SUCCESS;
} /* cltproTos2pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos3pri
 * cltproTos3pri is subid 27 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.27
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos3pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos3pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos3pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos3pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos3pri value.
     */

    return MFD_SUCCESS; /* cltproTos3pri value not illegal */
} /* cltproTos3pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos3pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos3pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos3pri undo.
     */
    /*
     * copy cltproTos3pri data
     * set rowreq_ctx->undo->cltproTos3pri from rowreq_ctx->data.cltproTos3pri
     */
    rowreq_ctx->undo->cltproTos3pri = rowreq_ctx->data.cltproTos3pri;


    return MFD_SUCCESS;
} /* cltproTos3pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos3pri_val
 *        A long containing the new value.
 */
int
cltproTos3pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos3pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos3pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos3pri value.
     * set cltproTos3pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos3pri = cltproTos3pri_val;

    return MFD_SUCCESS;
} /* cltproTos3pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos3pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos3pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos3pri undo.
     */
    /*
     * copy cltproTos3pri data
     * set rowreq_ctx->data.cltproTos3pri from rowreq_ctx->undo->cltproTos3pri
     */
    rowreq_ctx->data.cltproTos3pri = rowreq_ctx->undo->cltproTos3pri;

    
    return MFD_SUCCESS;
} /* cltproTos3pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos4pri
 * cltproTos4pri is subid 28 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.28
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos4pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos4pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos4pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos4pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos4pri value.
     */

    return MFD_SUCCESS; /* cltproTos4pri value not illegal */
} /* cltproTos4pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos4pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos4pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos4pri undo.
     */
    /*
     * copy cltproTos4pri data
     * set rowreq_ctx->undo->cltproTos4pri from rowreq_ctx->data.cltproTos4pri
     */
    rowreq_ctx->undo->cltproTos4pri = rowreq_ctx->data.cltproTos4pri;


    return MFD_SUCCESS;
} /* cltproTos4pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos4pri_val
 *        A long containing the new value.
 */
int
cltproTos4pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos4pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos4pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos4pri value.
     * set cltproTos4pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos4pri = cltproTos4pri_val;

    return MFD_SUCCESS;
} /* cltproTos4pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos4pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos4pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos4pri undo.
     */
    /*
     * copy cltproTos4pri data
     * set rowreq_ctx->data.cltproTos4pri from rowreq_ctx->undo->cltproTos4pri
     */
    rowreq_ctx->data.cltproTos4pri = rowreq_ctx->undo->cltproTos4pri;

    
    return MFD_SUCCESS;
} /* cltproTos4pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos5pri
 * cltproTos5pri is subid 29 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.29
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos5pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos5pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos5pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos5pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos5pri value.
     */

    return MFD_SUCCESS; /* cltproTos5pri value not illegal */
} /* cltproTos5pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos5pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos5pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos5pri undo.
     */
    /*
     * copy cltproTos5pri data
     * set rowreq_ctx->undo->cltproTos5pri from rowreq_ctx->data.cltproTos5pri
     */
    rowreq_ctx->undo->cltproTos5pri = rowreq_ctx->data.cltproTos5pri;


    return MFD_SUCCESS;
} /* cltproTos5pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos5pri_val
 *        A long containing the new value.
 */
int
cltproTos5pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos5pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos5pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos5pri value.
     * set cltproTos5pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos5pri = cltproTos5pri_val;

    return MFD_SUCCESS;
} /* cltproTos5pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos5pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos5pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos5pri undo.
     */
    /*
     * copy cltproTos5pri data
     * set rowreq_ctx->data.cltproTos5pri from rowreq_ctx->undo->cltproTos5pri
     */
    rowreq_ctx->data.cltproTos5pri = rowreq_ctx->undo->cltproTos5pri;

    
    return MFD_SUCCESS;
} /* cltproTos5pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos6pri
 * cltproTos6pri is subid 30 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.30
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos6pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos6pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos6pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos6pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos6pri value.
     */

    return MFD_SUCCESS; /* cltproTos6pri value not illegal */
} /* cltproTos6pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos6pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos6pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos6pri undo.
     */
    /*
     * copy cltproTos6pri data
     * set rowreq_ctx->undo->cltproTos6pri from rowreq_ctx->data.cltproTos6pri
     */
    rowreq_ctx->undo->cltproTos6pri = rowreq_ctx->data.cltproTos6pri;


    return MFD_SUCCESS;
} /* cltproTos6pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos6pri_val
 *        A long containing the new value.
 */
int
cltproTos6pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos6pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos6pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos6pri value.
     * set cltproTos6pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos6pri = cltproTos6pri_val;

    return MFD_SUCCESS;
} /* cltproTos6pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos6pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos6pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos6pri undo.
     */
    /*
     * copy cltproTos6pri data
     * set rowreq_ctx->data.cltproTos6pri from rowreq_ctx->undo->cltproTos6pri
     */
    rowreq_ctx->data.cltproTos6pri = rowreq_ctx->undo->cltproTos6pri;

    
    return MFD_SUCCESS;
} /* cltproTos6pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproTos7pri
 * cltproTos7pri is subid 31 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.31
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproTos7pri_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproTos7pri_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos7pri_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos7pri_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproTos7pri value.
     */

    return MFD_SUCCESS; /* cltproTos7pri value not illegal */
} /* cltproTos7pri_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproTos7pri_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproTos7pri_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproTos7pri undo.
     */
    /*
     * copy cltproTos7pri data
     * set rowreq_ctx->undo->cltproTos7pri from rowreq_ctx->data.cltproTos7pri
     */
    rowreq_ctx->undo->cltproTos7pri = rowreq_ctx->data.cltproTos7pri;


    return MFD_SUCCESS;
} /* cltproTos7pri_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproTos7pri_val
 *        A long containing the new value.
 */
int
cltproTos7pri_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproTos7pri_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos7pri_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproTos7pri value.
     * set cltproTos7pri value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproTos7pri = cltproTos7pri_val;

    return MFD_SUCCESS;
} /* cltproTos7pri_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproTos7pri_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproTos7pri_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproTos7pri undo.
     */
    /*
     * copy cltproTos7pri data
     * set rowreq_ctx->data.cltproTos7pri from rowreq_ctx->undo->cltproTos7pri
     */
    rowreq_ctx->data.cltproTos7pri = rowreq_ctx->undo->cltproTos7pri;

    
    return MFD_SUCCESS;
} /* cltproTos7pri_undo */

/*---------------------------------------------------------------------
 * prevail-mib::cltproEntry.cltproCommit
 * cltproCommit is subid 32 of cltproEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.10.1.32
 * Description:
Commit action node for saving current profile settings to databases.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param cltproCommit_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * cltproTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  true(1), false(2)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
cltproCommit_check_value( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCommit_val)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCommit_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid cltproCommit value.
     */

    return MFD_SUCCESS; /* cltproCommit value not illegal */
} /* cltproCommit_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (cltproTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * cltproTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
cltproCommit_undo_setup( cltproTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:cltproTable:cltproCommit_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup cltproCommit undo.
     */
    /*
     * copy cltproCommit data
     * set rowreq_ctx->undo->cltproCommit from rowreq_ctx->data.cltproCommit
     */
    rowreq_ctx->undo->cltproCommit = rowreq_ctx->data.cltproCommit;


    return MFD_SUCCESS;
} /* cltproCommit_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param cltproCommit_val
 *        A long containing the new value.
 */
int
cltproCommit_set( cltproTable_rowreq_ctx *rowreq_ctx, u_long cltproCommit_val )
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCommit_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set cltproCommit value.
     * set cltproCommit value in rowreq_ctx->data
     */
    rowreq_ctx->data.cltproCommit = cltproCommit_val;

    return MFD_SUCCESS;
} /* cltproCommit_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
cltproCommit_undo( cltproTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:cltproTable:cltproCommit_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up cltproCommit undo.
     */
    /*
     * copy cltproCommit data
     * set rowreq_ctx->data.cltproCommit from rowreq_ctx->undo->cltproCommit
     */
    rowreq_ctx->data.cltproCommit = rowreq_ctx->undo->cltproCommit;

    
    return MFD_SUCCESS;
} /* cltproCommit_undo */

/**
 * check dependencies
 *
 * This is useful for for tables which have dependencies between columns
 * (or rows, or tables). For example, two columns allocating a percentage
 * of something add up 100%.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * cltproTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @retval MFD_SUCCESS all the changes to the row are legal
 * @retval MFD_ERROR   one or more changes are not legal
 *
 * (see README-table-cltproTable if you don't have dependencies)
 */
int
cltproTable_check_dependencies(cltproTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    
    DEBUGMSGTL(("internal:cltproTable:cltproTable_check_dependencies","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:470:o: Check cltproTable row dependencies.
     * check that all new value are legal and consistent with each other
     */
    return rc;
} /* cltproTable_check_dependencies */

/** @} */
