/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "profileTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement profileTable get routines.
 * TODO:240:M: Implement profileTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table profileTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * prevail-mib::profileTable is subid 8 of modEoCMib.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.36186.8.8, length: 9
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement profileTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param proTid_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
profileTable_indexes_set_tbl_idx(profileTable_mib_index *tbl_idx, long proTid_val)
{
    DEBUGMSGTL(("verbose:profileTable:profileTable_indexes_set_tbl_idx","called\n"));

    /* proTid(1)/INTEGER/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->proTid = proTid_val;
    

    return MFD_SUCCESS;
} /* profileTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
profileTable_indexes_set(profileTable_rowreq_ctx *rowreq_ctx, long proTid_val)
{
    DEBUGMSGTL(("verbose:profileTable:profileTable_indexes_set","called\n"));

    if(MFD_SUCCESS != profileTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , proTid_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != profileTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* profileTable_indexes_set */


/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proIndex
 * proIndex is subid 2 of profileEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.36186.8.8.1.2
 * Description:
the index of profile entry in databases.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   0
 *
 * Ranges:  1 - 64;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proIndex data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proIndex_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proIndex_get( profileTable_rowreq_ctx *rowreq_ctx, long * proIndex_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proIndex_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proIndex_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proIndex data.
 * copy (* proIndex_val_ptr ) from rowreq_ctx->data
 */
    (* proIndex_val_ptr ) = rowreq_ctx->data.proIndex;

    return MFD_SUCCESS;
} /* proIndex_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proBase
 * proBase is subid 3 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.3
 * Description:
atheros eoc pib index.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 32;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proBase data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proBase_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proBase_get( profileTable_rowreq_ctx *rowreq_ctx, long * proBase_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proBase_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proBase_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proBase data.
 * copy (* proBase_val_ptr ) from rowreq_ctx->data
 */
    (* proBase_val_ptr ) = rowreq_ctx->data.proBase;

    return MFD_SUCCESS;
} /* proBase_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proMacLimit
 * proMacLimit is subid 4 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.4
 * Description:
EoC bridged hosts limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 8/16. Values:  hosts_limiting_disabled(0), one_host(1), two_hosts(2), three_hosts(3), four_hosts(4), five_hosts(5), six_hosts(6), seven_hosts(7), eight_hosts(8), null_hosts(65)
 *
 * Its syntax is HostsLimitingValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proMacLimit data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proMacLimit_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proMacLimit_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proMacLimit_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proMacLimit_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proMacLimit_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proMacLimit data.
 * copy (* proMacLimit_val_ptr ) from rowreq_ctx->data
 */
    (* proMacLimit_val_ptr ) = rowreq_ctx->data.proMacLimit;

    return MFD_SUCCESS;
} /* proMacLimit_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCableUplinkRate
 * proCableUplinkRate is subid 5 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.5
 * Description:
Cable uplink rate limiting in kbps.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proCableUplinkRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCableUplinkRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCableUplinkRate_get( profileTable_rowreq_ctx *rowreq_ctx, long * proCableUplinkRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCableUplinkRate_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCableUplinkRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCableUplinkRate data.
 * copy (* proCableUplinkRate_val_ptr ) from rowreq_ctx->data
 */
    (* proCableUplinkRate_val_ptr ) = rowreq_ctx->data.proCableUplinkRate;

    return MFD_SUCCESS;
} /* proCableUplinkRate_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCableDownlinkRate
 * proCableDownlinkRate is subid 6 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.6
 * Description:
Cable downlink rate limiting in kbps.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proCableDownlinkRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCableDownlinkRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCableDownlinkRate_get( profileTable_rowreq_ctx *rowreq_ctx, long * proCableDownlinkRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCableDownlinkRate_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCableDownlinkRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCableDownlinkRate data.
 * copy (* proCableDownlinkRate_val_ptr ) from rowreq_ctx->data
 */
    (* proCableDownlinkRate_val_ptr ) = rowreq_ctx->data.proCableDownlinkRate;

    return MFD_SUCCESS;
} /* proCableDownlinkRate_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proLocalAgingTime
 * proLocalAgingTime is subid 7 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.7
 * Description:
Local bridge aging time in minutes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proLocalAgingTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proLocalAgingTime_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proLocalAgingTime_get( profileTable_rowreq_ctx *rowreq_ctx, long * proLocalAgingTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proLocalAgingTime_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proLocalAgingTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proLocalAgingTime data.
 * copy (* proLocalAgingTime_val_ptr ) from rowreq_ctx->data
 */
    (* proLocalAgingTime_val_ptr ) = rowreq_ctx->data.proLocalAgingTime;

    return MFD_SUCCESS;
} /* proLocalAgingTime_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proRemoteAgingTime
 * proRemoteAgingTime is subid 8 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.8
 * Description:
Remote devie bridge table aging time in minutes.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proRemoteAgingTime data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proRemoteAgingTime_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proRemoteAgingTime_get( profileTable_rowreq_ctx *rowreq_ctx, long * proRemoteAgingTime_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proRemoteAgingTime_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proRemoteAgingTime_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proRemoteAgingTime data.
 * copy (* proRemoteAgingTime_val_ptr ) from rowreq_ctx->data
 */
    (* proRemoteAgingTime_val_ptr ) = rowreq_ctx->data.proRemoteAgingTime;

    return MFD_SUCCESS;
} /* proRemoteAgingTime_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proIgmpPri
 * proIgmpPri is subid 9 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.9
 * Description:
IGMP Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proIgmpPri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proIgmpPri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proIgmpPri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proIgmpPri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proIgmpPri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proIgmpPri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proIgmpPri data.
 * copy (* proIgmpPri_val_ptr ) from rowreq_ctx->data
 */
    (* proIgmpPri_val_ptr ) = rowreq_ctx->data.proIgmpPri;

    return MFD_SUCCESS;
} /* proIgmpPri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proUnicastPri
 * proUnicastPri is subid 10 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.10
 * Description:
Unicast Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proUnicastPri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proUnicastPri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proUnicastPri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proUnicastPri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proUnicastPri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proUnicastPri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proUnicastPri data.
 * copy (* proUnicastPri_val_ptr ) from rowreq_ctx->data
 */
    (* proUnicastPri_val_ptr ) = rowreq_ctx->data.proUnicastPri;

    return MFD_SUCCESS;
} /* proUnicastPri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proAvsPri
 * proAvsPri is subid 11 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.11
 * Description:
AV Stream Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proAvsPri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proAvsPri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proAvsPri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proAvsPri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proAvsPri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proAvsPri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proAvsPri data.
 * copy (* proAvsPri_val_ptr ) from rowreq_ctx->data
 */
    (* proAvsPri_val_ptr ) = rowreq_ctx->data.proAvsPri;

    return MFD_SUCCESS;
} /* proAvsPri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proMcastPri
 * proMcastPri is subid 12 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.12
 * Description:
Multicast Priority CAP value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proMcastPri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proMcastPri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proMcastPri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proMcastPri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proMcastPri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proMcastPri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proMcastPri data.
 * copy (* proMcastPri_val_ptr ) from rowreq_ctx->data
 */
    (* proMcastPri_val_ptr ) = rowreq_ctx->data.proMcastPri;

    return MFD_SUCCESS;
} /* proMcastPri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTbaPriSts
 * proTbaPriSts is subid 13 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.13
 * Description:
TX buffer based on priority status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTbaPriSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTbaPriSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTbaPriSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTbaPriSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTbaPriSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTbaPriSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTbaPriSts data.
 * copy (* proTbaPriSts_val_ptr ) from rowreq_ctx->data
 */
    (* proTbaPriSts_val_ptr ) = rowreq_ctx->data.proTbaPriSts;

    return MFD_SUCCESS;
} /* proTbaPriSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCosPriSts
 * proCosPriSts is subid 14 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.14
 * Description:
Map VLAN to Priority CAP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCosPriSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCosPriSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCosPriSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCosPriSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCosPriSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCosPriSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCosPriSts data.
 * copy (* proCosPriSts_val_ptr ) from rowreq_ctx->data
 */
    (* proCosPriSts_val_ptr ) = rowreq_ctx->data.proCosPriSts;

    return MFD_SUCCESS;
} /* proCosPriSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos0pri
 * proCos0pri is subid 15 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.15
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos0pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos0pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos0pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos0pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos0pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos0pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos0pri data.
 * copy (* proCos0pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos0pri_val_ptr ) = rowreq_ctx->data.proCos0pri;

    return MFD_SUCCESS;
} /* proCos0pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos1pri
 * proCos1pri is subid 16 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.16
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos1pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos1pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos1pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos1pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos1pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos1pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos1pri data.
 * copy (* proCos1pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos1pri_val_ptr ) = rowreq_ctx->data.proCos1pri;

    return MFD_SUCCESS;
} /* proCos1pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos2pri
 * proCos2pri is subid 17 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.17
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos2pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos2pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos2pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos2pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos2pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos2pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos2pri data.
 * copy (* proCos2pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos2pri_val_ptr ) = rowreq_ctx->data.proCos2pri;

    return MFD_SUCCESS;
} /* proCos2pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos3pri
 * proCos3pri is subid 18 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.18
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos3pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos3pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos3pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos3pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos3pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos3pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos3pri data.
 * copy (* proCos3pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos3pri_val_ptr ) = rowreq_ctx->data.proCos3pri;

    return MFD_SUCCESS;
} /* proCos3pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos4pri
 * proCos4pri is subid 19 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.19
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos4pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos4pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos4pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos4pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos4pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos4pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos4pri data.
 * copy (* proCos4pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos4pri_val_ptr ) = rowreq_ctx->data.proCos4pri;

    return MFD_SUCCESS;
} /* proCos4pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos5pri
 * proCos5pri is subid 20 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.20
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos5pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos5pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos5pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos5pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos5pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos5pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos5pri data.
 * copy (* proCos5pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos5pri_val_ptr ) = rowreq_ctx->data.proCos5pri;

    return MFD_SUCCESS;
} /* proCos5pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos6pri
 * proCos6pri is subid 21 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.21
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos6pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos6pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos6pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos6pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos6pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos6pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos6pri data.
 * copy (* proCos6pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos6pri_val_ptr ) = rowreq_ctx->data.proCos6pri;

    return MFD_SUCCESS;
} /* proCos6pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCos7pri
 * proCos7pri is subid 22 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.22
 * Description:
VLAN priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCos7pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCos7pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCos7pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCos7pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCos7pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCos7pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCos7pri data.
 * copy (* proCos7pri_val_ptr ) from rowreq_ctx->data
 */
    (* proCos7pri_val_ptr ) = rowreq_ctx->data.proCos7pri;

    return MFD_SUCCESS;
} /* proCos7pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTosPriSts
 * proTosPriSts is subid 23 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.23
 * Description:
Map TOS to Priority CAP.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTosPriSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTosPriSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTosPriSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTosPriSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTosPriSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTosPriSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTosPriSts data.
 * copy (* proTosPriSts_val_ptr ) from rowreq_ctx->data
 */
    (* proTosPriSts_val_ptr ) = rowreq_ctx->data.proTosPriSts;

    return MFD_SUCCESS;
} /* proTosPriSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos0pri
 * proTos0pri is subid 24 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.24
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos0pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos0pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos0pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos0pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos0pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos0pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos0pri data.
 * copy (* proTos0pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos0pri_val_ptr ) = rowreq_ctx->data.proTos0pri;

    return MFD_SUCCESS;
} /* proTos0pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos1pri
 * proTos1pri is subid 25 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.25
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos1pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos1pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos1pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos1pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos1pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos1pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos1pri data.
 * copy (* proTos1pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos1pri_val_ptr ) = rowreq_ctx->data.proTos1pri;

    return MFD_SUCCESS;
} /* proTos1pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos2pri
 * proTos2pri is subid 26 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.26
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos2pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos2pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos2pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos2pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos2pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos2pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos2pri data.
 * copy (* proTos2pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos2pri_val_ptr ) = rowreq_ctx->data.proTos2pri;

    return MFD_SUCCESS;
} /* proTos2pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos3pri
 * proTos3pri is subid 27 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.27
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos3pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos3pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos3pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos3pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos3pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos3pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos3pri data.
 * copy (* proTos3pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos3pri_val_ptr ) = rowreq_ctx->data.proTos3pri;

    return MFD_SUCCESS;
} /* proTos3pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos4pri
 * proTos4pri is subid 28 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.28
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos4pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos4pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos4pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos4pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos4pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos4pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos4pri data.
 * copy (* proTos4pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos4pri_val_ptr ) = rowreq_ctx->data.proTos4pri;

    return MFD_SUCCESS;
} /* proTos4pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos5pri
 * proTos5pri is subid 29 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.29
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos5pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos5pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos5pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos5pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos5pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos5pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos5pri data.
 * copy (* proTos5pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos5pri_val_ptr ) = rowreq_ctx->data.proTos5pri;

    return MFD_SUCCESS;
} /* proTos5pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos6pri
 * proTos6pri is subid 30 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.30
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos6pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos6pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos6pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos6pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos6pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos6pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos6pri data.
 * copy (* proTos6pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos6pri_val_ptr ) = rowreq_ctx->data.proTos6pri;

    return MFD_SUCCESS;
} /* proTos6pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTos7pri
 * proTos7pri is subid 31 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.31
 * Description:
TOS priority mappings.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  cap0(0), cap1(1), cap2(2), cap3(3)
 *
 * Its syntax is PriQueueValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTos7pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTos7pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTos7pri_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTos7pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTos7pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTos7pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTos7pri data.
 * copy (* proTos7pri_val_ptr ) from rowreq_ctx->data
 */
    (* proTos7pri_val_ptr ) = rowreq_ctx->data.proTos7pri;

    return MFD_SUCCESS;
} /* proTos7pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proSfbSts
 * proSfbSts is subid 32 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.32
 * Description:
Broadcast storm filter status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proSfbSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proSfbSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proSfbSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proSfbSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proSfbSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proSfbSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proSfbSts data.
 * copy (* proSfbSts_val_ptr ) from rowreq_ctx->data
 */
    (* proSfbSts_val_ptr ) = rowreq_ctx->data.proSfbSts;

    return MFD_SUCCESS;
} /* proSfbSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proSfuSts
 * proSfuSts is subid 33 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.33
 * Description:
Unknown unicast storm filter status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proSfuSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proSfuSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proSfuSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proSfuSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proSfuSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proSfuSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proSfuSts data.
 * copy (* proSfuSts_val_ptr ) from rowreq_ctx->data
 */
    (* proSfuSts_val_ptr ) = rowreq_ctx->data.proSfuSts;

    return MFD_SUCCESS;
} /* proSfuSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proSfmSts
 * proSfmSts is subid 34 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.34
 * Description:
Unknown multicast storm filter status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proSfmSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proSfmSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proSfmSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proSfmSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proSfmSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proSfmSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proSfmSts data.
 * copy (* proSfmSts_val_ptr ) from rowreq_ctx->data
 */
    (* proSfmSts_val_ptr ) = rowreq_ctx->data.proSfmSts;

    return MFD_SUCCESS;
} /* proSfmSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proSfRate
 * proSfRate is subid 35 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.35
 * Description:
Storm control rate level from 0 to 11.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 10/16. Values:  sfl_disabled(0), sfl_1kpps(1), sfl_2kpps(2), sfl_4kpps(3), sfl_8kpps(4), sfl_16kpps(5), sfl_32kpps(6), sfl_64kpps(7), sfl_128kpps(8), sfl_256kpps(9), sfl_512kpps(10), sfl_1024kpps(11)
 *
 * Its syntax is StromFilterValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proSfRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proSfRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proSfRate_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proSfRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proSfRate_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proSfRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proSfRate data.
 * copy (* proSfRate_val_ptr ) from rowreq_ctx->data
 */
    (* proSfRate_val_ptr ) = rowreq_ctx->data.proSfRate;

    return MFD_SUCCESS;
} /* proSfRate_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proVlanStatus
 * proVlanStatus is subid 36 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.36
 * Description:
CNU vlan status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proVlanStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proVlanStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proVlanStatus_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proVlanStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proVlanStatus_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proVlanStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proVlanStatus data.
 * copy (* proVlanStatus_val_ptr ) from rowreq_ctx->data
 */
    (* proVlanStatus_val_ptr ) = rowreq_ctx->data.proVlanStatus;

    return MFD_SUCCESS;
} /* proVlanStatus_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth1vid
 * proEth1vid is subid 37 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.37
 * Description:
CNU eth1 vlan id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 4094;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth1vid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth1vid_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth1vid_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth1vid_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth1vid_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth1vid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth1vid data.
 * copy (* proEth1vid_val_ptr ) from rowreq_ctx->data
 */
    (* proEth1vid_val_ptr ) = rowreq_ctx->data.proEth1vid;

    return MFD_SUCCESS;
} /* proEth1vid_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth2vid
 * proEth2vid is subid 38 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.38
 * Description:
CNU eth2 vlan id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 4094;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth2vid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth2vid_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth2vid_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth2vid_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth2vid_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth2vid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth2vid data.
 * copy (* proEth2vid_val_ptr ) from rowreq_ctx->data
 */
    (* proEth2vid_val_ptr ) = rowreq_ctx->data.proEth2vid;

    return MFD_SUCCESS;
} /* proEth2vid_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth3vid
 * proEth3vid is subid 39 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.39
 * Description:
CNU eth3 vlan id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 4094;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth3vid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth3vid_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth3vid_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth3vid_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth3vid_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth3vid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth3vid data.
 * copy (* proEth3vid_val_ptr ) from rowreq_ctx->data
 */
    (* proEth3vid_val_ptr ) = rowreq_ctx->data.proEth3vid;

    return MFD_SUCCESS;
} /* proEth3vid_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth4vid
 * proEth4vid is subid 40 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.40
 * Description:
CNU eth4 vlan id.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   0
 *   settable   1
 *
 * Ranges:  1 - 4094;
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth4vid data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth4vid_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth4vid_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth4vid_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth4vid_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth4vid_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth4vid data.
 * copy (* proEth4vid_val_ptr ) from rowreq_ctx->data
 */
    (* proEth4vid_val_ptr ) = rowreq_ctx->data.proEth4vid;

    return MFD_SUCCESS;
} /* proEth4vid_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proPortPriSts
 * proPortPriSts is subid 41 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.41
 * Description:
CNU port priority config status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proPortPriSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proPortPriSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proPortPriSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proPortPriSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proPortPriSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proPortPriSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proPortPriSts data.
 * copy (* proPortPriSts_val_ptr ) from rowreq_ctx->data
 */
    (* proPortPriSts_val_ptr ) = rowreq_ctx->data.proPortPriSts;

    return MFD_SUCCESS;
} /* proPortPriSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth1pri
 * proEth1pri is subid 42 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.42
 * Description:
cnu port pri value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth1pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth1pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth1pri_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth1pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth1pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth1pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth1pri data.
 * copy (* proEth1pri_val_ptr ) from rowreq_ctx->data
 */
    (* proEth1pri_val_ptr ) = rowreq_ctx->data.proEth1pri;

    return MFD_SUCCESS;
} /* proEth1pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth2pri
 * proEth2pri is subid 43 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.43
 * Description:
cnu port pri value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth2pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth2pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth2pri_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth2pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth2pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth2pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth2pri data.
 * copy (* proEth2pri_val_ptr ) from rowreq_ctx->data
 */
    (* proEth2pri_val_ptr ) = rowreq_ctx->data.proEth2pri;

    return MFD_SUCCESS;
} /* proEth2pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth3pri
 * proEth3pri is subid 44 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.44
 * Description:
cnu port pri value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth3pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth3pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth3pri_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth3pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth3pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth3pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth3pri data.
 * copy (* proEth3pri_val_ptr ) from rowreq_ctx->data
 */
    (* proEth3pri_val_ptr ) = rowreq_ctx->data.proEth3pri;

    return MFD_SUCCESS;
} /* proEth3pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth4pri
 * proEth4pri is subid 45 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.45
 * Description:
cnu port pri value.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth4pri data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth4pri_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth4pri_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth4pri_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth4pri_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth4pri_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth4pri data.
 * copy (* proEth4pri_val_ptr ) from rowreq_ctx->data
 */
    (* proEth4pri_val_ptr ) = rowreq_ctx->data.proEth4pri;

    return MFD_SUCCESS;
} /* proEth4pri_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proRxLimitSts
 * proRxLimitSts is subid 46 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.46
 * Description:
CNU port RX rate limiting status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proRxLimitSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proRxLimitSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proRxLimitSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proRxLimitSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proRxLimitSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proRxLimitSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proRxLimitSts data.
 * copy (* proRxLimitSts_val_ptr ) from rowreq_ctx->data
 */
    (* proRxLimitSts_val_ptr ) = rowreq_ctx->data.proRxLimitSts;

    return MFD_SUCCESS;
} /* proRxLimitSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCpuPortRxRate
 * proCpuPortRxRate is subid 47 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.47
 * Description:
CNU cpu port rx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proCpuPortRxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCpuPortRxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCpuPortRxRate_get( profileTable_rowreq_ctx *rowreq_ctx, long * proCpuPortRxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCpuPortRxRate_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCpuPortRxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCpuPortRxRate data.
 * copy (* proCpuPortRxRate_val_ptr ) from rowreq_ctx->data
 */
    (* proCpuPortRxRate_val_ptr ) = rowreq_ctx->data.proCpuPortRxRate;

    return MFD_SUCCESS;
} /* proCpuPortRxRate_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth1rx
 * proEth1rx is subid 48 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.48
 * Description:
eth1 port rx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth1rx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth1rx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth1rx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth1rx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth1rx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth1rx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth1rx data.
 * copy (* proEth1rx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth1rx_val_ptr ) = rowreq_ctx->data.proEth1rx;

    return MFD_SUCCESS;
} /* proEth1rx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth2rx
 * proEth2rx is subid 49 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.49
 * Description:
eth2 port rx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth2rx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth2rx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth2rx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth2rx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth2rx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth2rx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth2rx data.
 * copy (* proEth2rx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth2rx_val_ptr ) = rowreq_ctx->data.proEth2rx;

    return MFD_SUCCESS;
} /* proEth2rx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth3rx
 * proEth3rx is subid 50 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.50
 * Description:
eth3 port rx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth3rx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth3rx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth3rx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth3rx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth3rx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth3rx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth3rx data.
 * copy (* proEth3rx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth3rx_val_ptr ) = rowreq_ctx->data.proEth3rx;

    return MFD_SUCCESS;
} /* proEth3rx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth4rx
 * proEth4rx is subid 51 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.51
 * Description:
eth4 port rx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth4rx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth4rx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth4rx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth4rx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth4rx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth4rx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth4rx data.
 * copy (* proEth4rx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth4rx_val_ptr ) = rowreq_ctx->data.proEth4rx;

    return MFD_SUCCESS;
} /* proEth4rx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proTxLimitSts
 * proTxLimitSts is subid 52 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.52
 * Description:
CNU port TX rate limiting status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proTxLimitSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proTxLimitSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proTxLimitSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proTxLimitSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proTxLimitSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proTxLimitSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proTxLimitSts data.
 * copy (* proTxLimitSts_val_ptr ) from rowreq_ctx->data
 */
    (* proTxLimitSts_val_ptr ) = rowreq_ctx->data.proTxLimitSts;

    return MFD_SUCCESS;
} /* proTxLimitSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCpuPortTxRate
 * proCpuPortTxRate is subid 53 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.53
 * Description:
CNU cpu port tx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proCpuPortTxRate data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCpuPortTxRate_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCpuPortTxRate_get( profileTable_rowreq_ctx *rowreq_ctx, long * proCpuPortTxRate_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCpuPortTxRate_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCpuPortTxRate_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCpuPortTxRate data.
 * copy (* proCpuPortTxRate_val_ptr ) from rowreq_ctx->data
 */
    (* proCpuPortTxRate_val_ptr ) = rowreq_ctx->data.proCpuPortTxRate;

    return MFD_SUCCESS;
} /* proCpuPortTxRate_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth1tx
 * proEth1tx is subid 54 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.54
 * Description:
eth1 port tx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth1tx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth1tx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth1tx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth1tx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth1tx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth1tx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth1tx data.
 * copy (* proEth1tx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth1tx_val_ptr ) = rowreq_ctx->data.proEth1tx;

    return MFD_SUCCESS;
} /* proEth1tx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth2tx
 * proEth2tx is subid 55 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.55
 * Description:
eth2 port tx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth2tx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth2tx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth2tx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth2tx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth2tx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth2tx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth2tx data.
 * copy (* proEth2tx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth2tx_val_ptr ) = rowreq_ctx->data.proEth2tx;

    return MFD_SUCCESS;
} /* proEth2tx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth3tx
 * proEth3tx is subid 56 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.56
 * Description:
eth3 port tx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth3tx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth3tx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth3tx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth3tx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth3tx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth3tx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth3tx data.
 * copy (* proEth3tx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth3tx_val_ptr ) = rowreq_ctx->data.proEth3tx;

    return MFD_SUCCESS;
} /* proEth3tx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth4tx
 * proEth4tx is subid 57 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.57
 * Description:
eth4 port tx rate limiting.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 *
 * Its syntax is INTEGER (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (long)
 */
/**
 * Extract the current value of the proEth4tx data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth4tx_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth4tx_get( profileTable_rowreq_ctx *rowreq_ctx, long * proEth4tx_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth4tx_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth4tx_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth4tx data.
 * copy (* proEth4tx_val_ptr ) from rowreq_ctx->data
 */
    (* proEth4tx_val_ptr ) = rowreq_ctx->data.proEth4tx;

    return MFD_SUCCESS;
} /* proEth4tx_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proPsctlSts
 * proPsctlSts is subid 58 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.58
 * Description:
CNU port link status control status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proPsctlSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proPsctlSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proPsctlSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proPsctlSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proPsctlSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proPsctlSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proPsctlSts data.
 * copy (* proPsctlSts_val_ptr ) from rowreq_ctx->data
 */
    (* proPsctlSts_val_ptr ) = rowreq_ctx->data.proPsctlSts;

    return MFD_SUCCESS;
} /* proPsctlSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCpuPortSts
 * proCpuPortSts is subid 59 of profileEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.36186.8.8.1.59
 * Description:
CNU cpu port link status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCpuPortSts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCpuPortSts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCpuPortSts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCpuPortSts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCpuPortSts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCpuPortSts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCpuPortSts data.
 * copy (* proCpuPortSts_val_ptr ) from rowreq_ctx->data
 */
    (* proCpuPortSts_val_ptr ) = rowreq_ctx->data.proCpuPortSts;

    return MFD_SUCCESS;
} /* proCpuPortSts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth1sts
 * proEth1sts is subid 60 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.60
 * Description:
CNU eth1 port link status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proEth1sts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth1sts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth1sts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proEth1sts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth1sts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth1sts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth1sts data.
 * copy (* proEth1sts_val_ptr ) from rowreq_ctx->data
 */
    (* proEth1sts_val_ptr ) = rowreq_ctx->data.proEth1sts;

    return MFD_SUCCESS;
} /* proEth1sts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth2sts
 * proEth2sts is subid 61 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.61
 * Description:
CNU eth2 port link status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proEth2sts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth2sts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth2sts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proEth2sts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth2sts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth2sts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth2sts data.
 * copy (* proEth2sts_val_ptr ) from rowreq_ctx->data
 */
    (* proEth2sts_val_ptr ) = rowreq_ctx->data.proEth2sts;

    return MFD_SUCCESS;
} /* proEth2sts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth3sts
 * proEth3sts is subid 62 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.62
 * Description:
CNU eth3 port link status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proEth3sts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth3sts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth3sts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proEth3sts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth3sts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth3sts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth3sts data.
 * copy (* proEth3sts_val_ptr ) from rowreq_ctx->data
 */
    (* proEth3sts_val_ptr ) = rowreq_ctx->data.proEth3sts;

    return MFD_SUCCESS;
} /* proEth3sts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proEth4sts
 * proEth4sts is subid 63 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.63
 * Description:
CNU eth4 port link status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proEth4sts data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proEth4sts_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proEth4sts_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proEth4sts_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proEth4sts_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proEth4sts_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proEth4sts data.
 * copy (* proEth4sts_val_ptr ) from rowreq_ctx->data
 */
    (* proEth4sts_val_ptr ) = rowreq_ctx->data.proEth4sts;

    return MFD_SUCCESS;
} /* proEth4sts_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proCommit
 * proCommit is subid 64 of profileEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.36186.8.8.1.64
 * Description:
Commit action node for saving current profile settings to databases.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proCommit data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proCommit_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proCommit_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proCommit_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proCommit_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proCommit_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proCommit data.
 * copy (* proCommit_val_ptr ) from rowreq_ctx->data
 */
    (* proCommit_val_ptr ) = rowreq_ctx->data.proCommit;

    return MFD_SUCCESS;
} /* proCommit_get */

/*---------------------------------------------------------------------
 * prevail-mib::profileEntry.proRowStatus
 * proRowStatus is subid 65 of profileEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.4.1.36186.8.8.1.65
 * Description:
Profile entry row status.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   0
 *
 * Enum range: 2/8. Values:  true(1), false(2)
 *
 * Its syntax is TruthValue (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Extract the current value of the proRowStatus data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param proRowStatus_val_ptr
 *        Pointer to storage for a long variable
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
 */
int
proRowStatus_get( profileTable_rowreq_ctx *rowreq_ctx, u_long * proRowStatus_val_ptr )
{
   /** we should have a non-NULL pointer */
   netsnmp_assert( NULL != proRowStatus_val_ptr );


    DEBUGMSGTL(("verbose:profileTable:proRowStatus_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the proRowStatus data.
 * copy (* proRowStatus_val_ptr ) from rowreq_ctx->data
 */
    (* proRowStatus_val_ptr ) = rowreq_ctx->data.proRowStatus;

    return MFD_SUCCESS;
} /* proRowStatus_get */



/** @} */
